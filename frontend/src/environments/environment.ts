// Default values used by `ng serve` / `ng build` outside Docker.
// The Docker build regenerates this file from the VERSION build-arg (the git tag).
export const environment = {
  version: '0.0.0-local',
};
